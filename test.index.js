var assert = require('assert');
var fs = require('fs');

describe('testing color transforms of bitmap image -', function() {
  it('is a valid bitmap?', function() {
    
  });
  it('has the data been altered?', function() {

  });
  it('has proper palette offset?', function() {

  });
});
